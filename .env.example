# WebSocket URL (set this to your production backend WebSocket URL on Vercel/Render/Railway)
# Example: wss://syncboard-backend.onrender.com
VITE_WS_URL=

# Backend server port (defaults to 5000)
PORT=5000
